{% extends 'baseTemplate.html.twig' %}

{% block content %}
    <h3>{{ "list.name"|trans }}</h3>
    <ul>
        {% for album in albums %}
            <li>{{ album.name }}</li>
        {% endfor %}
    </ul>
{% endblock %}