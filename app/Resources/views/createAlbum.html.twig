{% extends 'baseTemplate.html.twig' %}

{% block content %}
    <div class="upper-box text-center loginscreen animated fadeInDown">
        <div>
            <h1 class="logo-name">CTF+</h1>
        </div>
        <h3>Bienvenido a Comparte Tus Fotos</h3>
        <p>Crea tu Album de fotografías y compartelas con quien quieras.
            <!--Continually expanded and constantly improved Inspinia Admin Them (IN+)-->
        </p>
        <!-- Formulario twig -->
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}